import React from "react";
import { Typography } from "@mui/material";

const Home = () => (
  <div style={{ textAlign: "center", paddingTop: "120px" }}>
    <Typography variant="h4" style={{ textAlign: "center", paddingTop: "120px" }}>
      Home
    </Typography>
  </div>
);

export default Home;
